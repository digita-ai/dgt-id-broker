{
  "name": "oidc-client",
  "version": "0.2.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "demo:auth0": "npm run dev -- --mode auth0 --port 3001",
    "demo:panva": "npm run dev -- --mode panva --port 3001",
    "demo:keycloak": "npm run dev -- --mode keycloak --port 3001"
  },
  "devDependencies": {
    "vite": "^2.1.0"
  },
  "dependencies": {
    "crypto-js": "^4.0.0",
    "dotenv": "^8.2.0",
    "esbuild": "^0.9.6",
    "jose": "^3.11.0",
    "js-sha256": "^0.9.0",
    "uuid": "^8.3.2"
  }
}
